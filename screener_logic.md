# 選股邏輯整理

> 更新日期：2026-05-27  
> 涵蓋範圍：`screener.py`（策略選股）、`tomorrow_strategy.py`（明日策略選股）

---

## 目錄

1. [兩套系統概覽](#1-兩套系統概覽)
2. [策略選股（screener.py）](#2-策略選股screenerpy)
3. [明日策略選股（tomorrow_strategy.py）](#3-明日策略選股tomorrow_strategypy)
4. [關鍵差異對照](#4-關鍵差異對照)

---

## 1. 兩套系統概覽

| 項目 | 策略選股 | 明日策略選股 |
|------|----------|-------------|
| 入口函式 | `run_screener_query()` | `run_tomorrow_strategy()` |
| 選股母體 | 僅**法人近5日買超**的股票 | DB 中**所有股票** |
| 技術基準線 | 簡單移動平均 MA20 / MA60 | Donchian 成本線 cost20 / cost60 |
| 大盤狀態來源 | `market_status.py`（外部模組） | 自行計算（從 TAIEX 日 K） |
| 大盤狀態代碼 | `normal_bull / hot_bull / overheated_bull / weak_market` | `strong_bull / healthy_pullback / high_overheated / weak_bounce / bear_break60` |
| 個股分級 | 無（直接用分數） | A / B1 / B2 / C 四級 |
| 核心輸出分類 | 明日優先 / 突破觀察 / 等回測 / 過熱警戒 / 暫不交易 | 明日可買 / 高優先觀察 / 其他觀察 / 排除 |
| 法人資料 | Step 1 硬篩（必要條件） | 不使用 |
| 流動性門檻 | amountMa20 ≥ 3000萬 **OR** volumeMa20 ≥ 1000張 | volMa20 ≥ 1000張 **OR** amountMa20 ≥ 5000萬（分 high/normal/low 層） |
| 分數系統 | 0–100，11 個評分項目 | 0–100，5 個維度 |
| 產業排行 | 有（`compute_industry_rankings()`） | 無 |
| 資料庫 | `stock_cache.db` | `stock_cache.db` |

---

## 2. 策略選股（screener.py）

### 2.1 六步驟流程

```
Step 1+2 → Step 3 → Step 4 → Step 5 → Step 6
法人篩選   行情過濾  讀K線    技術+評分   輸出排序
```

### 2.2 Step 1+2：法人近5日篩選（硬篩）

通過條件（同時滿足）：
- 近5日三大法人合計買超 > 0
- 近5日外資買超 > 0 **OR** 近5日投信買超 > 0

函式：`get_inst_5d_candidates()` → 回傳代號清單

### 2.3 Step 3：今日行情過濾

- 抓 TWSE + TPEx 當日報價
- 排除 `今日漲跌幅 < -3.5%`（可調整 `max_decline_pct`）
- 無報價的股票不因此被排除

### 2.4 Step 4：K 線載入

- 只讀 Step 3 存活的股票之 `daily_kbars` 與 `institutional_trading`
- 需 ≥ 62 根 K 線（確保能計算 60MA）

### 2.5 Step 5：技術分析

**補注今日 K 線**：若 TWSE 確認為今日資料且 DB 尚無今日 → 補一筆

**指標計算**：MA5 / MA10 / MA20 / MA60、MACD histogram、成交金額均線

**三道硬篩（任一不過即排除）**：

| 硬篩 | 條件 |
|------|------|
| 流動性 | amountMa20 ≥ 3000萬 **OR** volumeMa20 ≥ 1000張 |
| 多頭排列 | `close > MA20 > MA60` |
| 20日相對強度 | 近20日漲幅 > 大盤基準 1.5% |

### 2.6 ScoreEngine（0–100 分，11 項）

**加分項目：**

| # | 項目 | 最高分 | 條件 |
|---|------|--------|------|
| 1 | 趨勢多頭 | +20 | close > MA20 > MA60 |
| 2 | 20日相對強度 | +10 | 超越大盤 > +1.5% |
| 3 | 60日相對強度 | +10 | 近60日漲幅 > 4%（非硬篩，只加分） |
| 4 | 投信近5日買超 | +10 | 投信5日合計 > 0 |
| 5 | 外資近5日買超 | +8 | 外資5日合計 > 0 |
| 6 | 三大法人買超 | +7 | 三大5日合計 > 0 |
| 6b | 外資投信同步 | +5 | 兩者均 > 0 |
| — | 法人分數上限 | ≤ 25 | 4+5+6+6b 合計截斷至 25 |
| 7 | 乖離位置（bias20） | −25 ~ +10 | 0~3% +10；3~6% +8；6~10% +4；10~15% −15；>15% −25 |
| 8 | MACD 負柱收斂 | +10 | histogram < 0 且連續2~3日縮短 |
| 9 | K線轉強 | +10 | 收紅K 或站回5/10MA 或突破昨高 |
| 10 | 成交金額流動性 | +3 ~ +10 | 5日均額 ≥ 3億 +10；≥ 1億 +8；≥ 5000萬 +5 |
| 11 | 停損距離 | −20 ~ +10 | ≤3% +10；≤5% +7；≤6% +3；≤8% −10；>8% −20 |

**扣分項目（各 −10 分）：**

| 項目 | 條件 |
|------|------|
| 近5日漲幅過大 | 近5日漲幅 > 15% |
| 今日急漲 | 今日漲幅 > 6% |
| 長上影線 | 上影線比例 > 40% |
| 法人買超但不漲 | 三大合計 > 0 且今日收跌 |

**主力特徵加分（非過熱時套用，上限 +8）：**

| 特徵 | 加分 |
|------|------|
| 三人同買（外+投+自同日買超） | +3 |
| 外資/投信單邊主導 | +1 |
| 黃金滿貫（連買+同步） | +3 |
| 強勢雙雄（外資+投信連買） | +2 |
| 投信連買 ≥ 5日 | +3 |
| 投信連買 3~4日 | +2 |
| 外資連買 ≥ 5日 | +3 |
| 外資連買 3~4日 | +2 |

### 2.7 買點型態偵測

| 優先 | 型態 | 觸發條件 | 停損設定 |
|------|------|----------|---------|
| 1 | ⚠️ 過熱不交易 | bias20 > 15% 或 近5日漲 > 15% 或 上影線 > 40% | 不交易 |
| 2 | ⏳ 等回測 | bias20 > 10% | MA20 |
| 3 | 📍 回測20MA轉強 | 距MA20 < 3% 且低點不破MA20×0.98 且今日偏強 | min(MA20, 近5日低點) |
| 4 | 📍 回測前高不破 | 距近20日高點 < 3% 且收盤 ≥ 前高×0.97 | 前高 × 0.97 |
| 5 | 🚀 突破整理 | 突破近10/20日高點 且量 > 均量×1.2 | 當日最低 |
| 6 | 🚀 高檔續強 | 位於近20日高點區 且整理幅度 < 8% | 近10日最低 |
| 預設 | 🚀 高檔續強 | 以上皆不符 | 近10日最低 |

### 2.8 策略狀態分類

判斷順序（由上往下，首個符合者）：

| 狀態 | 標籤 | 核心條件 |
|------|------|---------|
| 過熱警戒 | 🔴 | bias20 > 15% 或 近5日漲 > 20% 或 今日漲 > 7% 或 上影線 > 40% 或 停損 > 8% |
| 等回測 | 🟡 | bias20 > 10% 或 停損 > 6%（且非過熱） |
| **明日優先** | 🟢 | 分數門檻依大盤調整（見下表）+ 多頭排列 + 流動性通過 |
| 突破觀察 | 🔵 | 分數 ≥ 65 或 帶量突破 且 流動性通過 |
| 等回測 | 🟡 | 分數 ≥ 50 |
| 暫不交易 | ⚪ | 其他 |

**明日優先 門檻（依大盤狀態）：**

| 大盤狀態 | 分數 | 乖離 | 停損 | 額外條件 |
|----------|------|------|------|---------|
| `overheated_bull` | ≥ 85 | ≤ 3% | ≤ 5% | 入場型態=回測20MA |
| `hot_bull` | ≥ 82 | ≤ 4% | ≤ 6% | 回測20MA 或 回測前高 |
| `normal_bull` | ≥ 80 | ≤ 6% | ≤ 6% | 多頭排列 |

### 2.9 輸出結構

```python
{
    "status":              "success",
    "data":                [...],          # 向後相容（全部）
    "buy_candidates":      [...],          # 明日優先，上限 20 檔
    "high_priority_watch": [...],          # 高優先觀察，上限 50 檔
    "other_watch":         [...],          # 其他觀察
    "excluded":            [...],
    "etf_candidates":      [...],
    "summary":             {...},
    "market_status":       {...},
}
```

### 2.10 排序邏輯

1. 策略狀態（明日優先 > 突破觀察 > 等回測 > 過熱警戒 > 暫不交易）
2. 分數由高到低
3. 有產業共振者優先（個股分數 ≥ 75 且產業分數 ≥ 80）
4. 成交金額由高到低
5. 停損距離由小到大
6. 法人佔比由高到低

### 2.11 產業排行（compute_industry_rankings）

對所有通過篩選的個股按產業分組，計算綜合分數：

```
產業分數 = avg_score    × 0.40
         + 20日強度分   × 0.25
         + 60日強度分   × 0.15
         + 法人佔比分   × 0.15
         + 突破集中度   × 0.05
         − 過熱比例懲罰 × 0.10
```

**產業狀態：** 過熱警戒 / 強勢主流 / 健康偏強 / 回測機會 / 突破集中 / 中性觀察 / 弱勢產業

個股若 `score ≥ 75 且 industryScore ≥ 80` → `hasIndustryResonance = true`

### 2.12 籌碼等級（tier）

| 等級 | 名稱 | 條件 |
|------|------|------|
| 1 | 黃金滿貫 | 投信連買 + 外資連買 + 今日三方同步 |
| 2 | 強勢雙雄 | 投信連買 + 外資連買 |
| 3 | 投信鎖碼 | 僅投信連買 |
| 4 | 外資鎖碼 | 僅外資連買 |
| 5 | 主力佈局 | 以上皆不符 |

---

## 3. 明日策略選股（tomorrow_strategy.py）

### 3.1 整體流程

```
取 TAIEX 日 K
     ↓
判大盤狀態（五狀態）
     ↓
讀 DB 所有個股 K 線
     ↓
每檔個股：商品類型分類 → 資料新鮮度排除 → 技術分析+分級 → 流動性過濾
     ↓
候選分類（依大盤狀態 × 個股分級）
     ↓
輸出
```

### 3.2 技術基準：Donchian 成本線

```
cost_n = (n 日最高 + n 日最低) / 2
```
與 MA（收盤價平均）不同，反映近期交易區間的中位。

### 3.3 大盤狀態判斷（calculate_market_regime）

以大盤 TAIEX 日 K 計算 cost20 / cost60，結合 MACD histogram 與量能判斷：

| 優先 | 狀態 | 標籤 | 條件 |
|------|------|------|------|
| 1 | `bear_break60` | 空頭破60 | `close < cost60` 或（`cost20 < cost60` 且 MACD負柱擴大） |
| 2 | `weak_bounce` | 弱勢反彈 | `cost60 ≤ close < cost20` 且 cost20下彎 且 hist < 0 且非負柱收斂 |
| 3 | `high_overheated` | 高檔過熱 | `close > cost20` 且距cost20 > 8% |
| 4 | `strong_bull` | 強多延伸 | `close > cost20 > cost60` 且（正柱放大 或 負柱收斂） |
| 5 | `healthy_pullback` | 健康回測 | `close ≥ cost60` 且距cost20 ∈ −5%~+5% 且負柱收斂 |

**各狀態操作策略：**

| 狀態 | 可買條件 | 倉位建議 |
|------|----------|---------|
| 強多延伸 | A級距cost20 ≤ 3%，MACD收斂，風報比 ≥ 1.5，非近60高點 | 正常倉位 |
| 健康回測 | A級距cost20 ≤ 8%，MACD收斂；B1站回cost20且量縮 | 正常偏保守 |
| 高檔過熱 | A級距cost20 ≤ 3%，MACD收斂（輕倉） | ≤ 1/3 倉 |
| 弱勢反彈 | 原則不買，A級列觀察 | 空倉等待 |
| 空頭破60 | 不可買 | 空倉 |

### 3.4 商品類型分類（classify_instrument）

| 類型 | 識別條件 | 處理 |
|------|----------|------|
| `reverse_etf` | 名稱含「反向/反1/放空/空方」或代號尾 R | 直接排除 |
| `leveraged_etf` | 名稱含「正2/2倍/槓桿/2X/正向2/兩倍」 | 直接排除 |
| `etn` | 名稱/產業含 ETN | 預設排除（INCLUDE_ETN=False） |
| `warrant` | 名稱含「購/售/權證」 | 預設排除 |
| `preferred_stock` | 名稱以甲特/乙特/丙特結尾或含「特別股」 | 排除 |
| `etf` | 代號以 00 開頭且長度 ≤ 7 | 進 etf_candidates（若A/B1且流動性OK）|
| `common_stock` | 以上皆不符 | 正常選股流程 |

### 3.5 個股分析（_analyze_stock）

**前置計算：**
- 計算 cost20 / cost60（Donchian）
- 計算 MACD（ema12/ema26/dif/dea/hist）
- 計算量能、成交金額（vol × close × 1000）
- 計算 cost20 五日斜率

**個股分級：**

| 級別 | 條件 |
|------|------|
| **A** | `close > cost20 > cost60`（三線多頭排列） |
| **B1** | `close > cost60` 且不符 A 也不符 B2 |
| **B2** | `close > cost60` 且 `close ≤ cost20` 且 cost20下彎 且（下跌放量 或 MACD負柱擴大） |
| **C** | `close ≤ cost60` |

**量能狀態：**

| 狀態 | 條件 |
|------|------|
| 高檔爆量長上影 | 量 > 均量×1.5 且 上影線比例 > 40% |
| 下跌放量 | 收黑K 且量 > 均量×1.2 |
| 量縮 | 量 < 均量 |
| 放量 | 量 > 均量×1.2 |
| 量平 | 其他 |

**停損設定：** 近3日最低點  
**風報比：** `(60日最高 − 現價) / (現價 − 停損)`，接近60日高點時標記無效

### 3.6 評分系統（_calculate_score，0–100）

| 維度 | 加分條件 | 扣分條件 |
|------|----------|---------|
| §11.1 成本線位置 | close > cost60 +15；close > cost20 +15；cost20 > cost60 +10 | close < cost60 −30 |
| §11.2 距cost20位置 | −2%~5% +10；dist60 在0~3% +8 | > 10% −10 |
| §11.3 量能 | 量縮 +8；放量站cost20 +7 | 下跌放量 −10；高檔爆量長上影 −10 |
| §11.4 MACD | 負柱收斂 +10；正柱放大/正柱 +8；正柱收斂 +4 | 負柱擴大 −10 |
| §11.5 風報比 | ≥ 2.0 +10；≥ 1.5 +6 | < 1.0 −10 |

### 3.7 流動性分層

| 層級 | 條件 |
|------|------|
| `high` | volMa20 ≥ 3000張 **且** amountMa20 ≥ 1億 |
| `normal` | volMa20 ≥ 1000張 **且** amountMa20 ≥ 5000萬 |
| `low_amount_pass` | volMa20 < 1000張 **但** amountMa20 ≥ 5000萬 |
| `low` | 以上皆不符 → **排除** |

### 3.8 候選分類（_classify_candidate）

**硬排除（觸發一項即排除，不看大盤）：**
- 分級 C
- 分級 B2
- 高檔爆量長上影
- 距cost20 > 12%

**動態排除（≥ 2 項觸發則排除）：**
- MACD 負柱擴大
- 下跌放量
- 風報比 < 1.0

**可買條件（依大盤狀態 × 個股分級）：**

| 大盤狀態 | 分級 | 條件 |
|----------|------|------|
| 強多延伸 | A | 距cost20 ≤ 3%，MACD收斂，有效風報比 ≥ 1.5，非近60高點，cost20_slope ≥ 0 |
| 健康回測 | A | 距cost20 ≤ 8%，MACD收斂，風報比OK |
| 健康回測 | B1 | 站回cost20，量縮，MACD收斂，風報比OK |
| 高檔過熱 | A | 距cost20 ≤ 3%，MACD收斂，風報比OK（輕倉） |
| 弱勢反彈 | 任何 | 不可買 |
| 空頭破60 | 任何 | 不可買 |

**觀察分類（A/B1 未達可買門檻時）：**

| 條件 | 分類 |
|------|------|
| A級，距cost20 ≤ 5%，無動態排除 | 高優先觀察 |
| A級，距cost20 > 5% 或有動態排除 | 其他觀察 |
| B1 站回cost20 且 MACD收斂 | 高優先觀察 |
| B1 未站回cost20 | 其他觀察 |
| 空頭破60 + A級 | 高優先觀察 |

### 3.9 輸出結構

```python
{
    "data_date":           "YYYY-MM-DD",
    "market_regime":       {...},          # 大盤五狀態完整資訊
    "buy_candidates":      [...],          # 普通股明日可買，上限 20 檔
    "etf_candidates":      [...],          # ETF 候選，上限 20 檔
    "high_priority_watch": [...],          # 高優先觀察，上限 50 檔
    "other_watch":         [...],          # 其他觀察，上限 100 檔
    "excluded":            [...],          # 含完整 debug 欄位
    "stats":               {...},
}
```

---

## 4. 關鍵差異對照

### 4.1 技術指標對比

| 面向 | 策略選股 | 明日策略選股 |
|------|----------|------------|
| 趨勢基準線 | MA20 / MA60（SMA，收盤均線） | cost20 / cost60（Donchian，高低中位） |
| 大盤判斷 | 外部模組（market_status.py） | 自行對 TAIEX 計算 |
| 法人資料 | 必要條件（Step 1 硬篩） | 不使用 |
| 個股分級 | 無 | A / B1 / B2 / C |
| 停損依據 | MA20 或買點型態低點 | 近3日最低點 |
| 風報比 | 加/扣分依據 | 可買/排除條件 |
| MACD | 評分 +10（負柱收斂） | 大盤判斷 + 個股分析 + 排除條件 |
| 乖離率計算 | `(close − MA20) / MA20 × 100` | `(close − cost20) / cost20 × 100` |

### 4.2 選股哲學差異

**策略選股（screener.py）**：  
「法人在哪裡布局 → 篩出來 → 技術是否支持明日進場」  
先用**法人籌碼**縮小母體，再用技術評分排序。強調籌碼面 × 技術面協同。

**明日策略選股（tomorrow_strategy.py）**：  
「大盤現在什麼狀態 → 找符合該狀態的股票結構」  
以**大盤 regime** 為主軸，股票分級，不需法人資料。強調大盤節奏 × 個股成本結構。

### 4.3 輸出分類對應

| 策略選股 | 明日策略選股 | 意義 |
|---------|------------|------|
| 明日優先（buy_candidates） | 明日可買（buy_candidates） | 明日可執行進場 |
| 突破觀察 + 等回測（high_priority_watch） | 高優先觀察 | 接近但未達進場條件 |
| 等回測 / 暫不交易（other_watch） | 其他觀察 | 趨勢強但需等待 |
| 過熱警戒 / 不符條件（excluded） | 排除（excluded） | 風險高或不符結構 |

---

*本文件由 Claude Code 自動整理自原始碼，以程式碼為準。*
